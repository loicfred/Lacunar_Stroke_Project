# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=True
FLASK_APP=src.web.app

# Server Settings
HOST=0.0.0.0
PORT=5000

# Security (CHANGE IN PRODUCTION)
SECRET_KEY=dev-secret-key-change-in-production

# Model Paths (if different from default)
# RANDOM_FOREST_MODEL=models/stroke_model.pkl